<%  parent.children.each do |child|
      if child.fixed_version_id == @version.id and not child.version_children.empty? %>
    <%= render :partial => "row", :locals => { :feature => parent, :color =>  color } %>
   <% end %>
<%  end %>  


  <%= render :partial => "loop", :locals => { :parent => feature, :color => 16777215 } %>
